package me.whereareiam.identica.feature.recognition.type;

/**
 * Signals that may be matched before a reconnecting player is recognized.
 */
public enum RecognitionSignal {
	USERNAME,
	IP,
	VIRTUAL_HOST
}
