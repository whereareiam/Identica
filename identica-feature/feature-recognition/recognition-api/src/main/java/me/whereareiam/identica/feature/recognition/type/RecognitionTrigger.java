package me.whereareiam.identica.feature.recognition.type;

/**
 * Trigger that caused a recognition attempt to be evaluated.
 */
public enum RecognitionTrigger {
	AUTOMATIC,
	EXPLICIT_PROVIDER_SELECTION
}
