### Configure provider features [step]

All feature implementations are compiled into Identica. Configure shared defaults under `features/` and override them under `providers[].features` for each provider. Providers must declare support for the selected behavior.

See [Features](../../configuration/features/index.mdx) for examples and the settings path map. Provider traits such as authoritative usernames describe identity guarantees and are not feature toggles.
